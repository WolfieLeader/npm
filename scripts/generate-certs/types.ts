import type { GenerateCertsOptions, generateCerts } from "generate-certs";

const _certsOpts: GenerateCertsOptions = { certsPath: "/tmp", activateLogs: false };
const _generateCerts: typeof generateCerts = {} as typeof generateCerts;
